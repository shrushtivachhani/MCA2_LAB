"""
write a program to create a series
and also create a series with array
and also using arange function which is same as range but create a array of it
"""
import numpy as np
# create a series
series = np.array([1, 2, 3, 4, 5])
print("Series: ", series)
# create a series with array
series_array = np.array([1, 2, 3, 4, 5])
print("Series with array: ", series_array)
print("Series with array: ", np.arange(1, 6))
# create using arange function which is same as range but create a array of it
